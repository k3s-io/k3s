name: Display Release

on:
  pull_request:
    # branches:
    #   - fix-release-dispach-error
  # push:
  #   branches:
  #     - fix-release-dispach-error

env:
  messsage1: ${{ secrets.K3S_RELEASE_TOKEN }}
  messsage2: ${{ vars.K3S_RELEASE_TOKEN }}

jobs:
  display-release-token:
    name: Display RELEASE_TOKEN
    runs-on: ubuntu-latest

    if: github.actor == 'johnatasr'

    permissions:
      contents: write
      pull-requests: write
      issues: none
    
    steps:
      - name: Run test validator
        run: |
          echo permission test : $messsage1   $messsage2  $K3S_RELEASE_TOKEN

  
